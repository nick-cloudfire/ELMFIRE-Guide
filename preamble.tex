\usepackage{graphicx} % Required for inserting images
\usepackage{array}
\usepackage{amssymb}
\usepackage{pifont}
\usepackage{adjustbox}
\usepackage[margin=1in]{geometry}

\usepackage{longtable}
\usepackage{pdflscape}   % rotates the *page* in the PDF viewer

\usepackage{booktabs}
\usepackage{multirow}
\usepackage{caption}

\usepackage{microtype} % better line breaking; reduces overfull boxes
\usepackage{xurl}      % allow line breaks in \url/\path at more characters (e.g., underscores)

\usepackage{setspace}
\usepackage{amsmath}

% Manual-like density
\setstretch{1.2} 
\setlength{\parskip}{2pt}
\setlength{\parindent}{1.2em}

\usepackage{enumitem}
\setlist{nosep}

\usepackage{subcaption} %subfigures

\usepackage[table,xcdraw]{xcolor}

% Inline "code"/parameter names that can break nicely at underscores, etc.
\newcommand{\code}[1]{\path|#1|}

% Last-resort flexibility to avoid text running into the margin
\emergencystretch=2em

\newenvironment{cfTable}[3]{%
    \begin{table}[htbp]
      \centering
      \caption{#1}
      \label{#2}
      \begin{minipage}{#3}
        \centering
        \small
}{%
      \end{minipage}
    \end{table}
}

% Hyperlink for inter-referenced texts
\usepackage{hyperref}

